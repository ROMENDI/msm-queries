<h1> Youngest director </h1>
<p> The youngest director in our dataset right now is 
    <% sorted_directors = Director.all.order({ :dob => :desc }) %>
    <% youngest = sorted_directors.at(0) %>
    <a href="/directors/<%= youngest.id %>"> <%= youngest.name %> </a>,
    born on <%= youngest.dob.strftime("%B %w, %Y")%>.
</p>
